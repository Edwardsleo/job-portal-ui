# Job Portal UI

A modern React 19 job portal application built with Vite, Tailwind CSS, and React Router, featuring job search, applications, and employer management with role-based access (job seeker, employer, admin).

To get started, run `npm install` followed by `npm run dev` to start the Vite development server.
